#include <iostream>
using namespace std;
#include <vector>

vector<int> foo(vector<int> arr)
{
    int count = 0;
    int fill = 0;
    for (int i = 0; i < arr.size(); i++)
    {
        
    }
    
}