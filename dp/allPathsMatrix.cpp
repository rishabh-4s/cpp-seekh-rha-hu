#include <bits/stdc++.h>
using namespace std;
vector<vector<int>> ans;
void solve(int i, int j, vector<vector<int> mat, int m, int n, vector<int> curr)
{
    if (i == m - 1 && j == n - 1)
    {
        ans.push_back(curr);
    }
}

int main()
{

    return 0;
}